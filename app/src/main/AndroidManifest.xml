<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.swe4550.dropin">
    <!--This allows the app to connect to the internet.-->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/drop_in_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/drop_in_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DropIn">
        <activity
            android:name=".SetUpProfile"
            android:exported="false" />
        <activity
            android:name=".SignUp"
            android:exported="false" />
        <activity
            android:name=".LogIn"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Discover"
            android:exported="false" />
    </application>

</manifest>